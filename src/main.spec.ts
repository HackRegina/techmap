import { TestBed, async } from '@angular/core/testing';
describe('Tech Map Data', () => {
  beforeEach(async(() => {
    TestBed.configureTestingModule({}).compileComponents();
  }));
  it('should be valid json.', async(() => {
    expect(true).toBeTruthy();
  }));
});
